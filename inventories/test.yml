---
all:
  hosts:
    srv1-test.fritz.box:
      ansible_user: administrator
      ansible_python_interpreter: /usr/bin/python3
    k3s1-test.fritz.box:
      ansible_user: administrator
      ansible_python_interpreter: /usr/bin/python3
    kube1-test.fritz.box:
      ansible_user: administrator
      ansible_python_interpreter: /usr/bin/python3
    kube2-test.fritz.box:
      ansible_user: administrator
      ansible_python_interpreter: /usr/bin/python3
  children:
    physical:
      hosts:
        srv1-test.fritz.box:
    docker:
      children:
        portainer:
          hosts:
            srv1-test.fritz.box:
    microk8s:
      children:
        microk8s_controller:
          hosts:
            kube1-test.fritz.box:
        microk8s_worker:
          hosts:
            kube2-test.fritz.box:
    k3s:
      children:
        k3s_controller:
          hosts:
            k3s1-test.fritz.box:
        k3s_worker:
          hosts:            
    ssmtp:
      hosts:
        srv1-test.fritz.box:
    virtualbox:
      hosts:
        srv1-test.fritz.box:
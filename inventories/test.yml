---
all:
  hosts:
    srv1-test.fritz.box:
      ansible_user: administrator
      ansible_python_interpreter: /usr/bin/python3
  children:
    cockpit:
      hosts:
        srv1-test.fritz.box:
    docker:
      children:
        gitlab:
          hosts:
            srv1-test.fritz.box:
    ssmtp:
      hosts:
        srv1-test.fritz.box:
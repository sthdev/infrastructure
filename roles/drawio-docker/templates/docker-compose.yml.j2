version: '3.8'

services:
  drawio:
    image: jgraph/drawio:{{drawio_version}}
    ports:
    - {{drawio_http_port}}:8080
    restart: always
    {# volumes:
    - ./PreConfig.j2:/usr/local/tomcat/webapps/draw/js/PreConfig.js
    - ./PostConfig.j2:/usr/local/tomcat/webapps/draw/js/PostConfig.js #}
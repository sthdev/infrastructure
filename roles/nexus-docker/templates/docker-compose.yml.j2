version: '3.7'

services:
  nexus:
    image: sonatype/nexus3:{{nexus_version}}
    ports:
    - {{nexus_port}}:8081
    volumes:
    - data:/nexus-data
    restart: always
    stop_grace_period: 1m

volumes:
  data: